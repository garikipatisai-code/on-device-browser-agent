/// <reference types="vite/client" />

// Injected by vite `define` at build time (a compact "MM-DD HH:MM" stamp). Shown in the panel
// header so a stale loaded extension is immediately obvious.
declare const __BUILD__: string;
